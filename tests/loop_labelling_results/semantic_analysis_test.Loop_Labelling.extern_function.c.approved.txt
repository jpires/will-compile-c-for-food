FunctionDecl(sub
             Storage(extern)
             FunType(RetType(Int)
                     Param(Int,Int)
             )
             (a, b)
             Return(
                    Binary(Subtract
                           Var(a)
                           Var(b)
                    )
             )
)

